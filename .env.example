# Database — Neon Postgres connection string
DATABASE_URL="postgresql://user:password@host.neon.tech:5432/graphsign?sslmode=require"

# JWT Secret — used for local auth token signing (min 32 chars)
JWT_SECRET="change-me-to-a-secure-random-string-at-least-32-chars"

# JWT token expiry
JWT_ACCESS_TOKEN_EXPIRY="15m"

# Email — Resend API key
RESEND_API_KEY=""

# Email sender address
EMAIL_FROM="noreply@emaildomain.com"

# Application URLs
API_URL="http://localhost:8787"
WEB_URL="http://localhost:3000"

# Node environment
NODE_ENV="development"

# Super Admin email(s) — comma-separated for multiple admins
SUPERADMIN_ID="kunal@graphomy.com"
